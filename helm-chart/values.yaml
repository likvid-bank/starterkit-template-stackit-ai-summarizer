replicaCount: 2

image:
  # Set at deployment time by CI (`--set image.repository=...`).
  repository: ""
  # Set at deployment time by CI (`--set image.digest=...`).
  digest: ""
  pullPolicy: IfNotPresent

# Stage used by the application and by optional stage overlays.
stage: prod
# Set at deployment time by CI (`--set appVersion=$GIT_SHA`).
appVersion: ""

containerPort: 8000

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

ingress:
  hostname: ""
  tlsSecretName: ""

stackitAiSecretName: stackit-ai

resources:
  # Overcommit on purpose for trial/demo: requests are set far below real usage
  # so many tenant deployments pack onto the small shared cluster without filling
  # up node capacity. Limits still cap real usage. Not intended for production.
  requests:
    cpu: 5m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi

livenessProbe:
  path: /health
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  path: /health
  initialDelaySeconds: 5
  periodSeconds: 5

podAnnotations: {}
podLabels: {}
